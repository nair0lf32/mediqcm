import 'package:flutter/material.dart';

class MyWelcome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        //TODO: ACTUALLY BUILD THE APP (LOL)
        );
  }
}
